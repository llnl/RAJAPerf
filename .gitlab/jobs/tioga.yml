###############################################################################
# Copyright (c) 2017-25, Lawrence Livermore National Security, LLC
# and RAJA Performance Suite project contributors.
# See the RAJAPerf/LICENSE file for details.
#
# SPDX-License-Identifier: (BSD-3-Clause)
#############################################################################

# Override reproducer section to define project specific variables.
.tioga_reproducer_vars:
  script:
    - !reference [.reproducer_vars, script]

########################
# Overridden shared jobs
########################
# We duplicate the shared jobs description and add necessary changes for RAJA.
# We keep ${PROJECT_<MACHINE>_VARIANTS} and ${PROJECT_<MACHINE>_DEPS} So that
# the comparison with the original job is easier.

# No overridden jobs so far.

############
# Extra jobs
############
# We do not recommend using ${PROJECT_<MACHINE>_VARIANTS} and
# ${PROJECT_<MACHINE>_DEPS} in the extra jobs. There is no reason not to fully
# describe the spec here.

cce_19_0_0:
  variables:
    SPEC: "~shared +openmp %cce@=19.0.0 ^blt@develop"
  extends: .job_on_tioga

rocmcc_6_4_1_hip_openmp:
  variables:
    SPEC: "~shared +rocm +openmp amdgpu_target=gfx90a %llvm-amdgpu@=6.4.1 ^hip@6.4.1 ^blt@develop"
    HSA_ENABLE_SDMA: "1"
    HSA_ENABLE_SDMA_COPY_SIZE_OVERRIDE: "0"
    GPU_FORCE_BLIT_COPY_SIZE: "0"
  extends: .job_on_tioga

rocmcc_6_4_1_hip_openmp_mpi:
  variables:
    SPEC: "~shared +rocm +openmp +mpi amdgpu_target=gfx90a %llvm-amdgpu@=6.4.1 ^hip@6.4.1 ^blt@develop"
    HSA_ENABLE_SDMA: "1"
    HSA_ENABLE_SDMA_COPY_SIZE_OVERRIDE: "0"
    GPU_FORCE_BLIT_COPY_SIZE: "0"
  extends: .job_on_tioga
